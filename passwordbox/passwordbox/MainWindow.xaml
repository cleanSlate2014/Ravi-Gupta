<Window x:Class="passwordbox.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <PasswordBox Style="{StaticResource passwordboxPrimary}" Margin="0,10,317,248" Width="500"></PasswordBox>
        <PasswordBox Style="{StaticResource passwordboxSuccess}" Margin="10,122,307,116"></PasswordBox>
        <PasswordBox Style="{StaticResource passwordboxDanger}" Margin="288,122,29,116"></PasswordBox>
        <PasswordBox Style="{StaticResource passwordboxWarning}" Margin="288,228,29,10"></PasswordBox>
        <PasswordBox Style="{StaticResource passwordboxDefault}" Margin="10,238,307,10"></PasswordBox>

     


    </Grid>
</Window>
